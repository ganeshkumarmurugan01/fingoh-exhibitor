{
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/upload", "destination": "/api/upload.cjs" },
    { "source": "/api/:slug*", "destination": "/api/proxy?slug=:slug*" }
  ]
}
